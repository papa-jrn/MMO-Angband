# Server App

This package will become the authoritative multiplayer server for:

- shared town state
- dungeon instance state
- session handling
- live chat routing

The current scaffold is intentionally minimal so the repository can be structured before dependency installation.

