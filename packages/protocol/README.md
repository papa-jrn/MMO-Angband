# Protocol Package

This package will hold shared message contracts between client and server.

It should eventually contain:

- message type definitions
- schema validation
- event names
- channel naming helpers

